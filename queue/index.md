# QUEUE


- queue-by-arrays-1.c : IT is normal fixed sized non circular queue with array with functions `enqueue`, `dequeue`, `front`, `rear`, `isempty`, `size`, `show`, `search`

- queue-by-ll-1.c : IT is normal dynamic sized non circular queue with linked list with functions `enqueue`, `dequeue`, `front`, `rear`, `isempty`, `size`, `show`, `search` , `clear`

- queue-by-ll-2.c : IT is normal dynamic sized circular queue with linked list with functions `enqueue`, `dequeue`, `front`, `rear`, `isempty`, `size`, `show`, `search` , `clear` in this i am just using the rear only the real nxt_ptr will containt the front ptr instead of teh null
